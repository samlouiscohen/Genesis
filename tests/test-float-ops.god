void init(){}
void update(int f){}

int main()
{
  float a;
  float b;
  float c;
  float d;
  float e;
  float f;
  int g;
  float h;

  a = 5.1;
  b = 4.7;
  c = a + b;
  d = a - b;
  e = a + a / d;
  f = a * 3;
  g = 3;
  h = a * g;

  printfl(a);
  printfl(b);
  printfl(c);
  printfl(d);
  printfl(e);
  printfl(f);
  printfl(h);
  return 0;
}
